python --version
python3 --version
pip show tensorflow
pip uninstall tensorflow
pip uninstall tensorflow-gpu
pip cache purge
pip install tensorflow
python -m venv my_thesis_env
pip install tensorflow
=============================
git status
git add .
git commit -m "Upload Date on github repository"
git push